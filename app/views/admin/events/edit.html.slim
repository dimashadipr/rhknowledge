.content-header
  .container-fluid
    .row.mb-2
      .col-sm-6
        h1.m-0.text-dark Events

.content
  .container-fluid
    .row
      .col-lg-9
        .card
          = form_with model: @event, url: admin_event_path(@event.id), local: true do |f|
            .card-body
              = render 'admin/events/forms/regular', f: f

            .card-footer
              = f.submit "Update", class: 'btn btn-flat btn-primary'

= render 'admin/events/js/form'
