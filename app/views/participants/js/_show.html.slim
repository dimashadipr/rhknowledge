= content_for :js
  javascript:
    $(document).on('change', '#paper_paper', function(){
      var allowedSize = 5 * 1024 * 1024;
      if (this.files && this.files[0]) {
        console.log(this.files[0].size);
        console.log(allowedSize);
        if (this.files[0].size > allowedSize) {
          alert('Ukuran file harus dibawah 1MB');
          this.value = '';
        }
      }
    });

    $(document).on('change', '#payment_proof', function(){
      var allowedSize = 5 * 1024 * 1024;
      if (this.files && this.files[0]) {
        console.log(this.files[0].size);
        console.log(allowedSize);
        if (this.files[0].size > allowedSize) {
          alert('Ukuran file harus dibawah 1MB');
          this.value = '';
        }
      }
    });
